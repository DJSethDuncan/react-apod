import React from "react";
import { render, screen } from "@testing-library/react";
import Photo from "../Photo";

describe("Video test", () => {
  it("renders", () => {
    expect(1).toBe(1);
  });
});
